@model IEnumerable<Student_Book.Link>
@using Student_Book.Controllers
@{
    ViewBag.Title = "Index";
}

<h2>Links</h2>
<br />
@if (User.IsInRole(RoleName.Admin) || User.IsInRole(RoleName.Manger))
{
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pdf_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Link1)
        </th>
        @*<th>
                @Html.DisplayNameFor(model => model.Link11.Pdf_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Link2.Pdf_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subject.Subject_Name)
            </th>*@
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pdf_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Link1)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.Link11.Pdf_Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Link2.Pdf_Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Subject.Subject_Name)
                </td>*@
            <td>
                @if (User.IsInRole(RoleName.Admin) || User.IsInRole(RoleName.Manger))
                {
                    @Html.ActionLink("Edit | ", "Edit", new { id = item.Id_Subject });
                    @Html.ActionLink("Details | ", "Details", new { id = item.Id_Subject });
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id_Subject });
                }
                else
                {
                    @Html.ActionLink("Details", "Details", new { id = item.Id_Subject });
                }
            </td>
        </tr>
    }

</table>
